<view class="scientis-item" wx:if="{{ item }}"
      bind:tap="pushToDetail">
  <view class="avatar-wrap">
    <view class="avatar">
      <image src="{{ item.avatar }}" mode="scaleToFill" lazy-load />
    </view>
  </view>
  <text class="username">{{ item.username }}</text>
</view>
