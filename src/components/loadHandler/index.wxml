<view class="load-handler">
  <text wx:if="{{ !loading && !disabled }}">{{ text }}</text>
  <text wx:if="{{ loading }}">{{ loadingText }}</text>
  <text wx:if="{{ disabled }}">{{ disabledText }}</text>
</view>