<view class="item {{ selected ? 'selected' : '' }}">

  <view class="icons">
    <icon class="icon" type="{{ icon }}" size="{{ selected ? 16 : 20 }}" />
  </view>

  <text class="title">{{ title }}</text>
  
</view>