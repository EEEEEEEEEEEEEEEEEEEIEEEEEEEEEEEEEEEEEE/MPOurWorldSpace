<scroll-view class="wrap" hidden="{{hidden}}" enable-back-to-top scroll-y>

  <page-header title="{{ page.title }}" title-sub="{{ page.slogan }}" />

  <scroll-nav items="{{ nav }}" />

</scroll-view>
