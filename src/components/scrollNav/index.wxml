<scroll-view class="scroll-nav" scroll-x="{{ true }}">
  <view class="inner">
    <block wx:for="{{ items }}" wx:key="{{ index }}">
      <text class="item {{ current === index ? ' current' : '' }}" 
            data-id="{{ index }}" 
            bindtap="itemChange">{{ item }}</text>
    </block>
  </view>
</scroll-view>
