/* 内容 */
.contents {
  bottom: 0;
  left: 0;
  position: absolute;
  right: 0;
  top: 270rpx;
}

/* 列表 */
.list {
  display: flex;
  flex-wrap: wrap;
  padding: 20rpx;
  position: relative;
}

.list .col {
  overflow: hidden;
  position: relative;
  width: 25%;
}
